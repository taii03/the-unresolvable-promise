import type { BaseTranslation } from '../i18n-types';

const es: BaseTranslation = {
	ping: {
		name: 'internacionalizacion',
		description: 'Muestra la latencia del bot.',
		reply: 'El ping es {ping:number}ms.',
	},
};

export default es;
